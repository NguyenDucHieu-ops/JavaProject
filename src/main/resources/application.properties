# ======================================================
# 🌱 APPLICATION INFO
# ======================================================
spring.application.name=NguyenDucHieu_2123110416
server.port=8080

# ======================================================
# 💾 DATABASE CONFIG (FOR DEPLOY RENDER)
# ======================================================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# ======================================================
# 🌍 CORS CONFIG (ReactJS)
# ======================================================
spring.mvc.cors.allowed-origins=http://localhost:3000,https://ten-react-app-cua-ban.netlify.app
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.mvc.cors.allowed-headers=*
spring.mvc.cors.allow-credentials=true

# ======================================================
# 📁 FILE UPLOAD CONFIG
# ======================================================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=uploads/

# ======================================================
# 📧 EMAIL CONFIG (Gmail OTP)
# ======================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bichnhaki5@gmail.com
spring.mail.password=xqfj sbff peyy kpjn

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.default-encoding=UTF-8

# ======================================================
# 🪄 FIX BEANS ERROR & JWT
# ======================================================
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false
spring.main.allow-circular-references=true

jwt.secret=dayLaChuoiBiMatCuaBanHayDoiNoDaiHon32KyTu

# ======================================================
# ☁️ CLOUDINARY CONFIG
# ======================================================
cloudinary.cloud_name=dd9eema2d
cloudinary.api_key=213813233216324
cloudinary.api_secret=D2NB2
cloudinary.upload_preset=hieukia
